<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Activity 启动闪屏主题;真正的 Material 3 ColorScheme 在 Compose 里走 WritingAppTheme。
      这里只需要一个"不报错"的主题让 manifest 引用。
    -->
    <style name="Theme.WritingApp" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- feishu-user-oauth:OAuthCodeReceiver 用，透明背景 -->
    <style name="Theme.WritingApp.Translucent" parent="android:Theme.Translucent.NoTitleBar" />
</resources>
package com.yy.writingwithai.app.ui.theme

import androidx.compose.material3.Typography

/**
 * M0 使用 Material 3 默认 Typography。
 * 后续 M3 / M5 按需要扩展 displayLarge / headlineMedium 等字体配置。
 */
internal val Typography = Typography()

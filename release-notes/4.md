floating-selection-toolbar · feishu 图片同步 · review r4 fix · 详情屏 polish

feat(floating-selection-toolbar): 笔记详情长按正文浮现 ⭐加入实体 + ✨AI 浮动工具栏(Box overlay + BottomCenter + windowInsetsPadding navigationBars)
fix(toHighlight): 实体下划线不再减去 titleLen(spanStart 一直是纯 content 偏移)
fix(entity-sheet-title): 实体卡片标题右侧类型显示中文(概念/人物/组织 等)而非英文 enum 名

feat(feishu-sync-image-support): 飞书同步支持图片附件 — 串行调 drive/v1/medias/upload_all(parent_type=docx_image, ≤20 MB),然后 docx/v1 appendChildren 用 block_type=27 把 image block 追加到 doc 末尾;upload/appendChildren 任一失败 → [图片:<attachmentId>] 文本占位符 + IMAGE_FAIL_PARTIAL event(ref 仍 SYNCED);image-text 不交叉(图片统一放最后)

fix(ai-stream): review r4 critical — AnthropicCompatibleAdapter.retry 显式 rethrow CancellationException(避免延迟协程取消信号传播);parseHttpDate 用 RFC_1123_DATE_TIME 替代非线程安全 SimpleDateFormat;NoteRepository.recomputeFlow collector 加协程生命周期注释

feat(note-detail-polish): 详情屏 polish — 拆解菜单 Loading 态保留菜单项(disabled + 进度文案)+ 顶部 LinearProgressIndicator;RelatedNotesSection 拆分实体/非实体 hit 关联(实体走 sheet,详情屏底部仅展示非实体 hit)

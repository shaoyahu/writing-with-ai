floating-selection-toolbar · selection toolbar 浮现修复 + 实体下划线错位修复

feat(floating-selection-toolbar): 笔记详情长按正文浮现 ⭐加入实体 + ✨AI 浮动工具栏(Box overlay + BottomCenter + imePadding)
fix(toHighlight): 实体下划线不再减去 titleLen(spanStart 一直是纯 content 偏移)
fix(entity-sheet-title): 实体卡片标题右侧类型显示中文(`概念`/`人物`/`组织` 等)而非英文 enum 名